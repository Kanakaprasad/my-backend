
# Basic env example
PORT=3000
NODE_ENV=development

# Redis (used for caching). Default expects a running local redis.
REDIS_URL=redis://127.0.0.1:6379

# Database (not required for this example; kept for future)
DATABASE_URL=postgres://pguser:pgpass@localhost:5432/appdb
